# Build-time variables (Render → Environment)
# Em dev, vazio = proxy Vite em /api e /uploads
# VITE_API_URL=http://localhost:8000

# Production (render.yaml):
# VITE_API_URL=https://watech-api.onrender.com
